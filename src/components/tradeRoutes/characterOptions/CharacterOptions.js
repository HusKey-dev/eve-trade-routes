import React, { Component } from 'react';

export class CharacterOptions extends Component {
    render() {
        return (
            <div>
                Character Options
            </div>
        )
    }
}

export default CharacterOptions;
