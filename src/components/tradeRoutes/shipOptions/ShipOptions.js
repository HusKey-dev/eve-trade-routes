import React, { Component } from 'react';

export class ShipOptions extends Component {
    render() {
        return (
            <div>
                Ship Options
            </div>
        )
    }
}

export default ShipOptions;
